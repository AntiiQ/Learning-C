#include <stdio.h>

int main()
{
    /*
    // you can scan multiple things at once
    int x, y ,z;
    float a, b, c;

    printf("Give me three numbers: ");
    scanf("%d %d %d", &x, &y, &z); // scanf ignores whitespace before the input
    printf("You gave me: %d, %d, %d\n", x, y, z);


    printf("Give me three floats: ");
    scanf("%f %e %g", &a, &b, &c); // dont forget the &
    printf("You gave me: %f, %f, %f", a, b, c);

    return 0;
    */

   //scanf isnt just input
   // it actuall scans a formated string from the user for the selected patterns
   // eg. scanf("%d/%d") looks for an integer followed by a / followed by an other integer
}